{
  "name": "arlo",
  "private": true,
  "engines": {
    "node": "10.15.3",
    "yarn": "1.15.2"
  },
  "scripts": {
    "build": "yarn --cwd arlo-client install && yarn --cwd arlo-client build",
    "heroku-postbuild": "yarn build"
  }
}
