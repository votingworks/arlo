version: 2.1
orbs:
  cypress: cypress-io/cypress@1
jobs:
  dependencies:
    docker:
      - image: circleci/python:3.7.3-node-browsers
        environment:
          DATABASE_URL: postgresql://root@localhost:5432/arlo-test
          FLASK_ENV: test
      - image: circleci/postgres:9.6.2-alpine
        environment:
          POSTGRES_USER: root
          POSTGRES_DB: arlo-test
    working_directory: ~/arlo
    steps:
      - checkout
      - run:
          parallelism: 4
          name: install
          command: |
            make install install-development
      - run:
          name: Install Chrome
          command: |
            curl -L -o google-chrome.deb https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
            sudo dpkg -i google-chrome.deb
            sudo sed -i 's|HERE/chrome\"|HERE/chrome\" --disable-setuid-sandbox|g' /opt/google/chrome/google-chrome
            rm google-chrome.deb
      - run:
          name: create data model
          command: |
            make resetdb
      - run:
          parallelism: 4
          name: lint server (pylint)
          command: |
            make lint-server
      - run:
          name: line client (yarn)
          command: |
            yarn --cwd client lint
      - run:
          name: type check
          command: |
            make typecheck-server
      - run:
          name: check formatting
          command: |
            make format-server
      - run:
          parallelism: 4
          name: server tests
          command: |
            circleci tests glob server/tests/**.py | circleci tests split --split-by=timings | xargs pipenv run pytest -n auto
      - run:
          parallelism: 4
          name: client tests
          command: |
            circleci tests glob client/src/**/*.test.* | circleci tests split --split-by=timings | yarn --cwd client test
      - run:
          name: wdio end-to-end tests
          command: |
            yarn --cwd client test:wdio
      - run:
          parallelism: 4
          name: cypress end-to-end tests
          command: |
            ./client/run-cypress-tests.sh

workflows:
  version: 2
  build-and-test:
    jobs:
      - dependencies
